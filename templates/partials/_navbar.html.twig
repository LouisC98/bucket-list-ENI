<header>
    <div class="logo">
        <img src="{{ asset("/images/logo.svg") }}" alt="logo" class="w-10">
        <a href="{{ path('app_home') }}">
            <h1 class="text-xl mx-1">Bucket List</h1>
        </a>
    </div>
    <button onclick="toggleMenu()" class="md:hidden cursor-pointer">
        <img src="{{ asset('/images/burger.svg') }}" alt="burger btn" id="burgerImg" class="w-8">
        <img src="{{ asset('/images/cross.svg') }}" alt="cross btn" id="crossImg" class="w-8 hidden">
    </button>
    <nav id="mobileMenu" class="absolute left-0 top-full w-full bg-white shadow-md hidden" aria-label="Menu navigation">
        <a href="{{ path('app_home') }}" class="block p-2">Accueil</a>
        <a href="{{ path('app_wish_new') }}" class="block p-2">Nouveau souhait</a>
        <a href="{{ path('app_about') }}" class="block p-2">À propos</a>
    </nav>
    <nav class="hidden md:flex gap-3" aria-label="Menu navigation">
        <a href="{{ path('app_home') }}">Accueil</a>
        <a href="{{ path('app_wish_new') }}">Nouveau souhait</a>
        <a href="{{ path('app_about') }}">À propos</a>
    </nav>
</header>
<script>
    function toggleMenu() {
        const mobileMenu = document.getElementById('mobileMenu');
        const burgerImg = document.getElementById('burgerImg');
        const crossImg = document.getElementById('crossImg');

        mobileMenu.classList.toggle('hidden');
        burgerImg.classList.toggle('hidden');
        crossImg.classList.toggle('hidden');
    }
</script>
