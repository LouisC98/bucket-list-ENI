{% extends 'base.html.twig' %}

{% block title %}Modifier souhait | {{ parent() }}{% endblock %}

{% block body %}
    <section class="w-full px-2 pt-5">
        <h1 class="text-center font-semibold text-xl my-3">Modifier souhait</h1>

        {{ include('wish/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

        <div class="mt-5 text-center">
            <a href="{{ path('app_home') }}" class="btn-secondary">Retour a la liste</a>
        </div>
    </section>
{% endblock %}
