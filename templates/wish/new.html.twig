{% extends 'base.html.twig' %}

{% block title %}Nouveau souhait | {{ parent() }}{% endblock %}

{% block body %}
    <section class="w-full px-2 pt-5">
        <h1 class="text-center font-semibold text-2xl text-shadow-lg mb-4">Nouveau souhait</h1>

        {{ include('wish/_form.html.twig') }}

        <div class="mt-5 text-center">
            <a href="{{ path('app_home') }}" class="btn-secondary">Retour a la liste</a>
        </div>
    </section>
{% endblock %}